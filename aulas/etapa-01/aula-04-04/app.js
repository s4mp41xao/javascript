// Em JavaScript, é comum a necessidade de converter valores de um tipo para outro. Existem várias maneiras de fazer isso:

// Declaração do valor que será convertido.
let score = '100'

// Number() //Converte um valor em um número.
score = Number(score)

// Exibindo o valor convertido.
// console.log(score + 1 ) // '101'

// typeof() // O operador verifica o tipo de um valor.
// console.log(typeof score) // 'Number'

// Declaração do valor que será convertido em string
const convertedNumber = String(97)

// Exibe o valor convertido e verifica o tipo do valor.
// console.log(convertedNumber, typeof convertedNumber) // 'string'

// Declaração do valor que será convertido.
const booleanConversion = Boolean(10)

// Exibe o valor convertido e verifica o tipo do valor.
console.log(booleanConversion, typeof booleanConversion) // 'boolean'