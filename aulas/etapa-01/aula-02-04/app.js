// Definindo a variável "email" que armazena um endereço de e-mail
const email = 'laracroft@rogermelo.com.br'

// Obtendo o índice da última ocorrência do caractere 'a' na string "email"
// const lastIndexOfA = email.lastIndexOf('a'); // '3'

// Utilizando o método "slice()" na string "email" para obter uma parte específica
// const emailSlice = email.slice(0, 9) // 'laracroft'

// Utilizando o método "replace()" na string "email" para substituir um caractere
// const emailReplace = email.replace('l', 'y') // 'yaracroft@rogermelo.com.br'

// Utilizando o método "replace()" com uma expressão regular para substituir todas as ocorrências de um caractere
// const emailReplace = email.replace(/l/g, 'y') // 'yaracroft@rogermelo.com.br'

// Exibindo a parte obtida no console
// console.log()
